<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>First page</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="min-h-screen w-full bg-zinc-800">

        <div class="form p-10 text-white"  >
            <form action="/create" method="post" >
                <input type="text" class="block w-full block rounded-xl bg-zinc-600  py-3 px-5 " name="title" placeholder="Title of the task">
                <textarea class="w-full block bg-zinc-600 mt-2 mb-2 rounded-xl resize-none px-5 py-2 "name="details"  placeholder="Write your task description" ></textarea>
                <input type="submit" class="bg-blue-700 rounded-md p-1 px-3" value="Create Task">
            </form>
        </div>

        <div class="tasks p-10 flex flex-wrap gap-3">
            <% if(files.length > 0) { %>
                <% files.forEach((val) => { %>
                    <div class="task w-80 px-3 py-2 rounded-md bg-zinc-600">
                        <h1 class="text-white tracking-tighter text-2xl"> <%= val %> </h1>
                        <a href="/file/<%= val%>" class="text-blue-600 mt-10 font-medium">Read More</a>
                    </div>
                <% }) %>
            <% } else { %>
                <h2 class="text-zinc-600">No task yet !!</h2>
            <% } %>
        </div>


    </div>
</body>
</html> 